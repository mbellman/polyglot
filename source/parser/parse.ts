import { IGrammar } from '../grammarizer/types';
import { ISyntaxTree } from './types';
import { IToken } from '../tokenizer/types';

export default function parse (tokens: IToken, grammar: IGrammar): ISyntaxTree {
  return {};
}
